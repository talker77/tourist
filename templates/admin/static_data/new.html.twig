{% extends 'admin/base.html.twig' %}
{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="grid">
                <div class="grid-header"></div>
                <div class="grid-body">

                    <h1>Create new StaticData</h1>

                    {{ include('admin/static_data/_form.html.twig') }}

                    <a href="{{ path('static_data_index') }}">back to list</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
