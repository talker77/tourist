{% extends 'admin/base.html.twig' %}
{% block content %}
        <div class="row">
            <div class="col-md-12">
                <div class="grid">
                    <div class="grid-header">Main Data</div>
                    <div class="grid-body">
                        {{ form_start(form) }}
                        {{ form_rest(form) }}
                            <input type="submit" class="btn btn-primary float-right mb-3" id="submit" value="Save"/>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>

        {% for block in blocks  %}


            {{ render(controller(
                'App\\Controller\\Admin\\DocumentController::saveBlock',
                {'document':document.id,'block':block.id}
            )) }}
        {% endfor %}

{% endblock %}
{% block javascripts %}
    <script src="{{ asset('ripple/src/vendors/summernote/dist/summernote-lite.min.js') }}"></script>
        <script src="{{ asset('js/ckeditor/ckeditor.js') }}"></script>
        <script src="{{ asset('js/ckeditor/init.js') }}"></script>
        <script>

        </script>
{% endblock %}


{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('ripple/src/vendors/summernote/dist/summernote-lite.css') }}">
{% endblock %}
